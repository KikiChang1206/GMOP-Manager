# ============================================================
# 固定取件機器人 — cron 排程範例
# ============================================================
# 安裝方式:
#   crontab -e
# 然後貼入下面這行(記得把路徑改成實際安裝位置)。
#
# 說明:
#   - 建議設在「系統當天可下單、且早於司機排班」的時間(設計文件第5節)
#   - 下例為每天早上 07:30 執行一次
#   - 一定要先 cd 到專案目錄,cron 的工作目錄不是專案目錄
#   - 輸出導到 log 檔,方便事後查(程式本身也會寫 logs/daily.log)
#
# 分 時 日 月 週   指令
30 7 * * *  cd /opt/fixed-pickup-robot && /usr/bin/node src/run.js >> logs/cron.log 2>&1
