# 好馬吉 → FedEx 建立貨件 自動填入

一鍵把好馬吉訂單的寄／收件資料帶進 FedEx 「建立貨件」表單。

## 安裝與使用

1. 瀏覽器安裝 **Tampermonkey**（Chrome / Edge 擴充功能）。
2. Tampermonkey → 建立新腳本 → 貼上 `fedex-autofill.user.js` 全部內容 → 儲存。
3. **好馬吉訂單頁**（Step1 已展開）右下角面板 → 按 **① 抓取本張訂單**（資料複製到剪貼簿）。
4. **FedEx 建立貨件頁** 右下角面板 → 按 **② 自動填入 FedEx**。
5. 送出前務必自己核對：**服務、幣別、報關方式、寄件品項** 等 FedEx 必填。

> 不想裝擴充功能，也可把 `(function(){…})()` 主體包成書籤（bookmarklet）；但 Tampermonkey 對 SPA 與剪貼簿權限最穩，建議用它。

## 欄位對應規則

### 寄件地（Ship From）— 混合：姓名用客人的，地址用好馬吉固定值

| FedEx 欄位 | 來源 |
|---|---|
| 聯絡人姓名 | 好馬吉「寄件人資訊 · 姓名」（客人）|
| 公司 | 好馬吉「寄件人資訊 · 英文姓名 / 公司名稱」（客人）|
| 電話號碼 | **固定** `0277309898` |
| 電子郵件 | **固定** `gmcs@goodmaji.com` |
| 國家/地區 | **固定** `Taiwan` |
| 地址行 1 | **固定** `1F., No. 18, Wuquan 7th Rd.,` |
| 地址行 2 | **固定** `Wugu Dist.,` |
| 郵遞區號 | **固定** `248020` |
| 城市 | **固定** `New Taipei City` |
| 這是住宅地址 | 不勾 |

### 收件地（Ship To）— 全部用好馬吉「收件人資訊」

| FedEx 欄位 | 來源 |
|---|---|
| 聯絡人姓名 | 收件人 · 姓名 |
| 公司 | 收件人 · 公司名稱 |
| 電話號碼 | 收件人 · 聯絡方式1 |
| 電子郵件 | 收件人 · E-mail |
| 國家/地區 | 收件人 · 國別（代碼自動轉 FedEx 顯示文字，如 `US → United States`）|
| 郵遞區號 | 收件人 · 郵編 |
| 地址行 1 / 城市 / 州 | 由收件人 · 地址「逗號自動拆解」：最後段=國家、倒數第二=州、再前=城市、其餘=街道 |

## 需要調整時

- **倉庫地址／電話變了** → 改 `fedex-autofill.user.js` 最上面的 `FIXED_SENDER`。
- **某欄沒填到** → 打開瀏覽器 Console 看 `找不到欄位: xxx`，多半是該頁標籤文字不同；把訊息回報即可微調 `FEDEX_LABELS` / `GM_*_LABELS`。
- **多國家** → 在 `COUNTRY_MAP` 補上代碼對應。

## 限制

- 這是「畫面自動填字」，非 FedEx 官方 API；FedEx 若大改版面（標籤文字改變）需同步更新標籤設定。
- 地址拆解以「逗號分段」為準，遇到非標準地址請人工核對城市／州。
